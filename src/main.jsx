import React from 'react';
import ReactDOM from 'react-dom/client';
import '../style.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div style={{ textAlign: 'center', marginTop: '100px', fontFamily: 'system-ui, sans-serif' }}>
      <h1>The One</h1>
      <p>React + Vite setup is complete and running!</p>
      <p>Chatbot interface coming in the next steps.</p>
    </div>
  </React.StrictMode>
);
